{
	"sshAddress": "http://localhost:4000",
	"commands" : [
		{
			"timeout": 1000,
			"command": "connection",
			"promise": "function(response,foglet){foglet.ssh.signaling.emit('logs', 'I am connected');}",
			"args": [],
			"finally": "function(foglet){console.log(foglet);}"
		}
	]
}
