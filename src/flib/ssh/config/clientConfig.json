{
	"sshAddress": "http://localhost:4000",
	"commands" : [
		{
			"timeoutBeforeEach": 5000,
			"timeout": 0,
			"command": "function(foglet){ foglet.connection().then(function(data){ foglet.ssh.signaling.emit('logs', 'connected'); }); }"
		},
		{
			"timeoutBeforeEach": 0,
			"timeout": 25000,
			"command": "function(foglet){ foglet.connection().then(function(data){ foglet.ssh.signaling.emit('logs', foglet.getNeighbours()); }); }"
		}
	]
}
